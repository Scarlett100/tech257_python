import random
import math
#random generates random number between 0 and 1 by default unless you clarify boundaries otherwise
print(random.random())

num_float = 23.66
print(num_float)
print(math.ceil(num_float)) #rounds number up
print(math.floor(num_float)) #rounds number down
print(math.pi)
